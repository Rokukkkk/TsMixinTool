import * as UE from "ue";

export class /*BlueprintName*/ extends TsClass<UE/*BlueprintPathName_Dot*/_C> {
    Initialize(uObj: UE.Object): void {
        super.Initialize(uObj);

        /*PlaceholderOverrides*/
    }
}